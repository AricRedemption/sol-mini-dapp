# 项目开发规范 (Clean Code)

## 核心原则

- **代码简洁之道**: 保持函数短小，职责单一。
- **注释准则**:
  - 移除不必要的注释，尤其是解释“代码在做什么”的注释。
  - 注释应当只用来解释“为什么”这么做。
  - 命名是最好的注释：变量、函数、类名应具有描述性。
- **封装与解耦**: 组件和函数应根据职责适当封装，避免大型单体文件。
- **避免魔法值**: 使用具名常量（Constants）替代代码中的字面量（Magic Numbers/Strings）。
- **彻底移除无效代码**: 严禁将不再使用的代码放在注释中，应当直接删除。

## 技术规范

- **TypeScript**:
  - 严格限制 `any` 的使用。
  - 使用 `import type` 导入类型定义。
- **React**:
  - 逻辑复杂时提取自定义 Hook。
  - 确保 Hook 依赖项完整。
  - 使用 `useCallback` 稳定传递给子组件的函数。
